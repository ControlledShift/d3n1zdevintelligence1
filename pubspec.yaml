name: d3n1zdev_intelligence
description: Offline AI Chat with DeepSeek
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  dio: ^5.4.0
  path_provider: ^2.1.1
  flutter_mediapipe_chat: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true